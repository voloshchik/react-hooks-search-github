import React from "react";

const Search = () => {
  return (
    <div className="form-group">
      <input
        type="text"
        className="form-control"
        placeholder="Введите ник польдзователя..."
      />
    </div>
  );
};

export default Search;
